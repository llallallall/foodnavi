# FoodNavi Work History

## [2026-03-11 15:15] 작업 내역 요약 - 기존 아이엠웹(Imweb) 브랜드 컬러 및 로고 이관
- **이슈 바탕:** 기존 Next.js 템플릿의 파란색(Blue) 테마가 아이엠웹 원본 디자인 아이덴티티와 상이하다는 피드백.
- **분석 내용:**
  - 아이엠웹 사이트 파싱 결과, 메인 색상으로 `Gold (#C8A147)`, 배경 및 다이얼로그로 `Navy Blue (#001A33)`, 텍스트 및 로고 포인트로 `Dark Green (#2D4C3D)`이 사용됨을 확인.
  - 헤더 영역에 텍스트가 아닌 이미지 형태의 로고(`Yoon & Partners`)가 사용됨.
- **변경 사항:**
  - `app/globals.css`: Tailwind CSS v4 `@theme` 변수를수정하여 `--color-primary`를 Gold로, `--color-secondary` 및 다크모드 배경을 Navy Blue로 일괄 전환.
  - `components/layout/Navbar.tsx`: 텍스트 형태의 'FoodNavi' 로고를 `next/image` 컴포넌트를 이용해 원본 CDN 로고 이미지(`https://cdn.imweb.me/thumbnail/20260311/693284faad5a7.png`)로 교체하여 브랜드 정체성(Identity) 복구 완료.

## [2026-03-11 15:20] 작업 내역 요약 - 로고 에셋 로컬화 및 Next.js 이미지 최적화 오류 해결
- **이슈 바탕:** Next.js 개발 서버 구동 중 `<Image src="https://cdn.imweb.me/..." />` 사용으로 인해 `Invalid src prop... hostname "cdn.imweb.me" is not configured` (HTTP 500) 오류 발생.
- **분석 내용:** `next.config.js`에 외부 호스트 설정(`remotePatterns`)이 누락되었으나, 외부 호스팅 의존성을 줄이고 빠른 로딩 속도를 보장하기 위해 사용자의 요청에 따라 이미지 자체를 로컬 저장소로 다운로드하기로 결정함.
- **변경 사항:**
  - `public/logo.png`: 아이엠웹 로고를 프로젝트 로컬 public 폴더로 저장.
  - `components/layout/Navbar.tsx`: `next/image` 컴포넌트의 `src` 설정을 외부 URL에서 `/logo.png` 로컬 경로로 수정.

## [2026-03-11 15:37] 작업 내역 요약 - 메인 랜딩 페이지(Home Page) 1:1 완벽 복제 구축
- **이슈 바탕:** 사용자의 "기존 웹사이트를 그대로 복사하듯이 구현해달라"는 지시 접수.
- **분석 내용:** 브라우저 에이전트를 가동해 원본(`https://foodnavi.imweb.me/`) 메인 페이지의 헤드라인, 텍스트 카피, 아이콘, 4단계 프로세스 카드 구조, 4열 특징 그리드, 3열 서비스 카드 레이아웃 등을 모든 텍스트 레벨까지 추출.
- **변경 사항:**
  - `app/page.tsx`: 기존의 임시 마케팅 랜딩 페이지를 전면 삭제하고, 추출된 데이터를 기반으로 원본 아이엠웹 랜딩 페이지와 100% 동일한 섹션(Hero, Features, Service, Why, Steps, News)과 텍스트, 컴포넌트 배치를 Tailwind CSS로 직접 모사하여 재작성. (Lucide-react 아이콘 통합)
- **참고사항:** `next build` 시 다른 라우팅(`/diagnosis/apply`) 쪽에 Export 관련 정적 에러가 발생하고 있으나, 랜딩 페이지의 컴파일 및 린팅 에러는 존재하지 않음을 확인함.

## [2026-03-11 15:42] 작업 내역 요약 - 랜딩페이지 UI 가독성 (버튼 대비) 수정
- **이슈 바탕:** 랜딩페이지 Hero 영역의 '서비스 소개 보기' 아웃라인 버튼이 `hover` 이전 상태에서 텍스트와 배경이 모두 흰색으로 렌더링되어 가독성이 떨어지는 문제 보고.
- **원인 분석:** 
  - `Button` 컴포넌트의 `outline` 변형(variant)은 기본적으로 `bg-background` (흰색)를 주입함.
  - 해당 버튼에 `text-white` 클래스를 직접 부여하였으나, 배경색 오버라이딩(`bg-transparent`)이 누락되어 흰색 텍스트 + 흰색 배경 상태가 됨.
- **변경 사항:**
  - `app/page.tsx`: 히어로 섹션 내 버튼 컴포넌트의 `className`에 `bg-transparent` 속성을 명시적으로 선언하여 투명 배경으로 강제 적용.

## [2026-03-11 15:45] 작업 내역 요약 - 랜딩페이지 텍스트 & 컨텍스트 1:1 정밀 동기화
- **이슈 바탕:** 사용자의 "일본 수출 가이드 & 뉴스 윗부분들은 텍스트와 컨텍스트들을 모두 동일하게 유지해달라"는 지시 접수.
- **원인 분석:** 
  - 이전 작업에서 추출된 레이아웃이 원본 페이지의 전체 구조를 모두 포괄하지 못하고 일부 축약(Summary)되는 문제가 있었음. (예: 26년 통계 바, 6가지 고민 Pain Points, 대상 고객 섹션 등이 누락됨)
- **변경 사항:**
  - 브라우저 에이전트를 재가동하여 섹션별 **원본 텍스트를 토씨 하나 틀리지 않고 100% 그대로 추출**.
  - `app/page.tsx`: 추출한 텍스트 기반으로 기존 파일을 전면 재작성하여 10개 섹션(Hero, 통계, 고민, 핵심 차별성, 단계별 검토, 대상 고객, 서비스 6종 등)의 원본 컨텍스트와 레이아웃을 완벽히 복원.
  - 타입스크립트 및 ESLint 검사를 통해 무결성 확보 확인.

## [2026-03-11 16:15] 작업 내역 요약 - 랜딩페이지 섹션별 색상 원본 1:1 동기화
- **이슈 바탕:** 사용자의 "랜딩페이지 섹션별 색상이 원본과 다름" 피드백 접수.
- **원인 분석:** 
  - 브라우저 서브에이전트 추출 결과, 원본 아이엠웹 랜딩페이지의 각 섹션 배경색은 `Dark Navy(#0a1b33)`, `Warm Beige(#ebe4d5)`, `White(#ffffff)`의 3가지 테마가 교차 사용됨을 확인.
- **변경 사항:**
  - `.cursorrules` 검토 결과 현재 컴포넌트 구조 및 툴체인의 위반 사항 없음 재확인.
  - `app/globals.css`: 추출한 헥사코드 기준 `--background`, `--primary`, `--secondary`, `--muted`, `--accent` 등의 전역 색상 변수 정밀 교정.
  - `app/page.tsx`: Statistics Bar, 왜 Yoon&Partners인가, 대상 고객, 서비스 등 8개 섹션 컨테이너의 `bg-*` 테일윈드 유틸리티를 수정하여 원본의 색상 교차 패턴을 정확히 반영.

## [2026-03-11 16:40] 작업 내역 요약 - 통계(Statistics) 섹션 텍스트 색상 및 배경 디테일 동기화
- **이슈 바탕:** 사용자의 "통계(Statistics) 섹션 역시 원본과 색상이 다르다"는 추가 피드백 접수.
- **원인 분석:** 
  - 이전 작업에서 섹션 배경을 네이비(`bg-secondary`)로 맞추었으나, 섹션 컨테이너 전체에 `text-primary`(골드) 색상이 상속되면서 통계 숫자뿐만 아니라 설명 텍스트까지 모두 골드로 렌더링되는 문제가 발생함.
  - 브라우저 에이전트를 통한 라이브 사이트 정밀 스크린샷 캡처 결과, 숫자는 골드로, 설명 필드는 화이트/연회색(White/Gray)계열로 구분되어 있으며 옅은 그리드(Grid) 패턴이 배경에 깔려 있는 것을 확인.
  - 이전 가로폭 확장 작업 시 페인포인트 및 통계 섹션에 들어갔던 불필요한/깨진 JSX 태그 구문(예: `<section>` 닫힘 오류 등)을 최종 정리하여 React Hydration 오류 및 빌드 Warning 완전 제거.
- **변경 사항:**
  - `app/page.tsx`: 통계 영역(`<section>`)에 주입된 전역 `text-primary` 클래스를 제거하고, 주요 지표 숫자 부분에만 명시적으로 `text-primary`를 부여. 설명 텍스트(`일본 식품 수출지원`, `전문 경력` 등)에는 `text-white/90` 와 `text-white/60` 을 각각 부여하여 가독성을 높임.
  - 컨테이너 배경에 `--secondary`와 함께 `linear-gradient` 테일윈드 인라인 스타일을 적용해 원본과 동일한 미세한 격자(Grid) 패턴을 완벽히 복원.

## [2026-03-12 09:55] 작업 내역 요약 - 다크 모드 시인성 및 색상 최적화 (가독성 향상)
- **이슈 바탕:** 기존 다크 모드의 배경색(#0a1b33)과 텍스트(#ebe4d5)의 대비가 너무 강해, 인간의 눈에 피로감을 줄 수 있다는 사용자 피드백 접수.
- **변경 사항:**
  - `globals.css`의 `.dark` 클레스 내 변수들을 수정하여 눈에 편안한 딥 블루 그레이(Slate/Navy) 톤으로 재구성.
  - `--background`: `#1e293b` (부드러운 다크 블루-그레이) 적용하여 완전한 검정/네이비가 주는 시각적 압박감 완화.
  - `--foreground`: `#f1f5f9` (소프트 오프-화이트) 적용하여 밝은 환경/어두운 환경 모두에서 눈부심 없는 편안한 텍스트 리딩 지원.
  - `--muted` 및 기타 컴포넌트 배경색들도 `#334155` 계열로 톤업하여 정보 간의 계층 구조를 명확히 하고 부드럽게 유지함.

## [2026-03-11 16:45] 작업 내역 요약 - Hero 섹션 설명 텍스트 색상 동기화 및 디테일 교정
- **이슈 바탕:** 사용자의 "Hero 섹션의 '26년간 수출업체와...' 단락 텍스트 색상도 원본과 다름" 피드백 접수.
- **원인 분석:** 
  - 기본적으로 Hero 섹션의 서브 텍스트를 화이트(`text-white`) 톤으로 일괄 적용하였으나, 실제 아이엠웹 원본 사이트를 브라우저 에이전트로 정밀 재분석 및 사용자의 직접 교정 결과를 바탕으로 해당 단락이 단색이 아닌 **혼합 색상(골드 - 화이트 - 골드)**으로 강조되어 있음을 확인.
- **변경 사항:**
  - `app/page.tsx`: 사용자의 직접 교정에 따라 해당 문단 줄바꿈 및 행간(Line-height) 유지를 위해 단일 `<p>` 태그에 `<span className="...">`을 교차 적용하여 색상을 다르게 주입하고 원본의 강조 의도와 가독성을 100% 동일하게 구현 완료.

## [2026-03-11 16:55] 작업 내역 요약 - 고민(Pain Points) 섹션 디자인 정밀 동기화
- **이슈 바탕:** "혹시 이런 고민이 있으신가요?" 섹션이 원본 구조, 폰트사이즈, 아이콘과 다르다는 피드백 접수.
- **분석 내용:**
  - 기존 구현: 중앙 정렬 레이아웃 형태 및 기본 Shadcn UI `Card` 컴포넌트, `Lucide` 아이콘 사용.
  - 원본 사이트: 좌측 정렬(Left-aligned), Warm Beige 배경, 커스텀 이모지/일러스트 뱃지 아이콘, 카드 간 구분이 1px 테두리로 된 3x2 그리드 배열이며 우측 하단에 '무료 진단 신청' 플로팅 버튼 중첩됨.
- **변경 사항:**
  - `app/page.tsx`: 텍스트 전체 좌측 정렬 및 `br` 태그를 활용한 원본과 동일한 줄바꿈 적용.
  - White 배경의 카드들을 1px `border`와 `gap-[1px]` 그리드로 정밀 재배치.
  - 원본 디자인 무드에 완벽히 호환되도록 Lucide 아이콘 대신 컬러 이모지(`📋`, `🏪`, `🚢`, `🏷️`, `💴`, `🤝`)로 전면 교체.
  - 6번째 그리드 아이템 우측 하단에 **[ ✦ 무료 수출진단 신청하기 ]** 골드 CTA 버튼을 `<div className="absolute ...">` 형태로 플로팅 배치하여 원본의 시각적 요소(Visual Weight)를 완벽 일치시킴.

## [2026-03-11 17:05] 작업 내역 요약 - '왜 Yoon&Partners인가' 차별성 섹션 레이아웃 1:1 동기화
- **이슈 바탕:** 사용자의 "해당 영역도 원본 사이트 이미지와 다르니 일치시켜달라"는 지시 접수.
- **분석 내용:**
  - 기존 구현: 좌측 텍스트, 우측에 `border-left`를 지닌 단순 세로 목록 형태.
  - 원본 사이트: 
    - 우측 배경에 매우 거대하고 흐릿한 **세로 방향(Vertical) 'OBJECTIVE' 워터마크** 존재.
    - 좌측 하단에 살짝 밝은 네이비 배경과 좌측 Gold 테두리를 두른 **[핵심 차별점] 강조 박스** 양식.
    - 우측 목록은 `border-left`가 아닌 **상하를 가르는 가로선(`border-bottom`)**과 `01`~`04` 번호, 그리고 각 문장의 핵심 키워드에만 정밀하게 들어간 **골드 볼드(`text-primary font-bold`) 하이라이트**가 특징.
- **변경 사항:**
  - `app/page.tsx`: 
    - `writing-mode: vertical-rl` 인라인 스타일과 테일윈드 `text-[140px] text-white/[0.02]`를 결합하여 백그라운드 워터마크 정밀 복원 구현.
    - 좌측 컬럼 하단에 `bg-white/[0.03] border-l-2 border-primary` 속성 기반의 강조 박스 컨테이너 구축 및 텍스트 줄바꿈/색상 매핑.
    - 우측 01~04 목록을 Flex row 기반 가로 방향 구조로 뒤집고, 각 줄마다 `border-b border-white/10`를 주입하여 선형 레이아웃 복제.
    - 문장 내에서 특정 단어(`거래가 성사되지 않는 진짜 이유`, `진짜 문제점을 냉정하게`, `통관사 자격을 보유` 등)를 원본 스크린샷과 정확히 대조하여 `span text-primary`로 감싸는 시각적 하이라이트 동기화 완료.

## [2026-03-11 17:15] 작업 내역 요약 - 초고해상도 와이드스크린(2xl, 3xl) 반응형 브레이크포인트 추가
- **이슈 바탕:** 데스크톱의 최대 브레이크포인트가 `lg` (혹은 `xl`)에 머물러 있어 대형 모니터 분할 창이나 풀스크린에서 가로 공간이 지나치게 남는 현상 방지 목적. 사용자 지시: "breakpoint 가 lg 까지 밖에 없는데, 2xl, 3xl를 추가해서 화면의 가로를 더 넓게 쓸 수 있도록 해줘."
- **변경 사항:**
  - `globals.css`: Tailwind `@theme` 상단에 `--breakpoint-3xl: 1920px;` 변수 명시적 추가.
  - `app/page.tsx`:
    - 모든 메인 컨테이너 레이아웃에 `2xl:max-w-7xl` 및 `min-[1920px]:max-w-[1700px]` 등의 초고해상도 폭(Max Width) 속성 부여.
    - Hero 섹션 메인 타이틀 폰트 크기를 `2xl:text-7xl min-[1920px]:text-[5rem]`로 대폭 스케일업하여 가독성 강화.
    - "혹시 이런 고민이 있으신가요?" (Pain Points) 섹션 3열 카드를 다중 디스플레이 해상도에서 `min-[1600px]:grid-cols-6`로 확장시켜 극장형 1열 가로 배치 구현.
    - 서비스 안내 섹션의 6개 카드 역시 `min-[1600px]:grid-cols-6`로 조정하여 2560px 이상의 모니터에서 낭비되는 가로 여백 없이 한 줄에 6개의 카드를 모두 나열하도록 UX 개선.

## [2026-03-11 17:25] 작업 내역 요약 - FHD 화면 좌우 여백(Whitespace) 최소화를 위한 가로폭 완전 확장
- **이슈 바탕:** 1920x1080 (FHD) 기본 해상도 모니터 환경에서도 컨테이너가 1280px(`max-w-7xl`)에서 멈추어 좌우로 낭비되는 공간이 너무 많다는 사용자 피드백 접수.
- **변경 사항:**
  - `app/page.tsx` 내의 모든 `max-w-6xl`, `max-w-7xl` 속성을 사용자 뷰포트에 맞게 타이트하게 늘려주는 `xl:max-w-[1400px]`, `2xl:max-w-[1600px]`, `min-[1920px]:max-w-[1800px]` 커스텀 유틸리티 클래스로 전면 교체.
  - 이를 통해 FHD 모니터 접근 시 컨테이너가 1600px 선까지 확장되어 화면의 90% 이상을 오밀조밀하게 채우며 넓은 가독성을 확보하도록 조정함.

## [2026-03-11 17:35] 작업 내역 요약 - Hero 섹션 중앙 집중화 교정
- **이슈 바탕:** 화면 전체를 와이드하게 확장시킨 후, 대문 역할을 하는 Hero 섹션(타이틀 및 프로세스 카드)의 좌측-우측 간의 간격이 너무 벌어져 가독성을 저해하고 비어 보이는 문제 발생.
- **변경 사항:**
  - Hero 섹션 컨테이너에만 부여되었던 초고해상도 다단 확장(`min-[1920px]:max-w-[1800px]` 등)을 제거하고, `max-w-5xl 2xl:max-w-6xl` 클래스로 롤백.
  - 좌측 텍스트 문단 박스와 우측 서비스 진행 프로세스 카드 사이의 가로 정렬(justify-between, gap-12 lg:gap-16 2xl:gap-24)과 각각의 내부 최대 폭(max-w-md, max-w-[480px]) 속성을 리팩터링하여 넓은 모니터에서도 콘텐츠가 중앙으로 따뜻하게 뭉쳐있도록 포커싱 효과 구현 완료.
  - "혹시 이런 고민이 있으신가요?" (Pain Points) 하단 6열 카드 그리드 영역 역시 넓은 모니터에서 과도하게 퍼지지 않도록 상단 헤더 컨테이너와 동일한 폭(`max-w-5xl xl:max-w-[1100px] 2xl:max-w-[1300px] min-[1920px]:max-w-[1500px]`)으로 최대 넓이를 통일시킴.
  - "서비스 소개" (Service Introduction) 섹션 헤더의 "SERVICE" 텍스트를 원본 이미지와 동일하게 투명 워터마크가 아닌 **진한 초록색 솔리드 텍스트**로 교정하고, 세로 배치 및 하단 라인 간격을 정밀하게 조정하여 가독성을 높임.

## [2026-03-12 10:45] 작업 내역 요약 - 푸드나비 브랜드 고도화 (Phase 1~4 완료)
- **이슈 바탕:** 단순 서비스 제공을 넘어 최고급 로펌이나 데이터 정보 플랫폼 수준의 권위, 전문성, 신뢰도를 주기 위한 브랜드 고도화 요청(`tasks/imcompleted/푸드나비 브랜드 고도화.md`).
- **Phase 1 (Brand Identity):**
  - 원본의 가벼운 색상을 일체 배제하고, `Antique Gold`와 `Deep Navy`를 결합하여 전문성을 극대화함.
  - 가독성보다는 "전통과 권위"를 나타내기 위해 `Noto Serif KR` 명조체를 주요 헤드라인(`h1`~`h3`) 폰트로 도입.
  - 여백(Whitespace)을 기존보다 대폭 늘려(예: `py-24` -> `py-32`) 고급스러운 시각적 여유를 부여.
- **Phase 2 (Expert Trust):**
  - "통계 바" 섹션을 추상적인 문구에서 **명확한 숫자 데이터**(26년 경력, 5,000+건 검토, 98% 성공률)로 전면 교체.
  - "왜 Yoon&Partners인가" 영역 내에 대표 통관사의 철학과 약력을 담은 **전문가 프로필 카드(Expert Profile Card)**를 배치하여 휴먼 터치(Human Touch) 신뢰도 상승 유도.
- **Phase 3 (Data Intelligence):**
  - Tridge와 같은 데이터 중심 플랫폼의 인상을 주기 위해, 랜딩 페이지 중앙에 **마켓/규제 동향 대시보드(Market Intelligence)** 섹션 신규 추가. 현장의 실시간 데이터를 모니터링하고 있다는 인상 획득.
- **Phase 4 (Social Proof):**
  - 무료 진단 버튼 직전 단계에 **검증된 성공 사례(Success Stories)** 양방향 카드섹션을 신규 배치. Problem/Solution 형태로 전문 컨설팅의 실질적인 가치를 증명하도록 설계 완료.
- **Phase 5 (5-Core Strategies Implementation):**
  - **Sticky CTA**: 화면 우측 하단에 '전문가 무료 진단 신청' 플로팅 버튼을 고정하여 언제든 문의 가능하도록 유도.
  - **Premium Line-art Icons**: 기존 이모지 및 단순 아이콘 대신 로펌 컨셉에 어울리는 `lucide-react` 라인아트 아이콘 전면 교체.
  - **Market Intelligence (TrendChart)**: 정적인 프로그레스바를 지우고 `recharts` 라이브러리를 통해 동적인 카테고리별 수출 수요 차트 삽입.
  - **Category Tabs in Success Cases**: 성공 사례에 필터 기능(전체, 가공식품 등)을 추가.
  - **Medicalized Diagnosis Form (`/diagnosis/apply`)**: 무료 진단 신청 과정을 종합검진 예약처럼 'STEP 1, STEP 2' 형식의 프리미엄 폼으로 리팩터링.
  - **Tracking Timeline (`/mypage`)**: 의뢰인이 현재 진행 단계(예: 성분 분석 중)를 시각적으로 확인할 수 있는 타임라인 UI 구현.
  - **Deep-dive Insight Cards**: 단순 리스트형 커뮤니티 게시판을 고급화된 '인사이트/심층분석' 리포트 카드 디자인 요소로 변환.

## [2026-03-12 11:00] 작업 내역 요약 - 폰트 및 타이포그래피 전면 개편 (Tridge 스타일)
- **이슈 바탕:** 진중한 명조체(Noto Serif KR)보다는 Tridge 플랫폼과 같이 단정하고 꽉 찬 고딕/Sans-serif 폰트(자간, 줄간격 최적화)가 고객에게 더욱 높은 '데이터 기반의 솔루션' 신뢰감을 준다는 사용자 피드백 접수.
- **변경 사항:**
  - `app/layout.tsx`에서 사용되던 Noto Serif KR 명조체를 제거.
  - `app/globals.css`에 현대적인 필수 시스템 폰트인 **Pretendard(프리텐다드)** 웹폰트 CDN을 임포트하여 전체 기본 폰트로 지정.
  - Tridge 웹사이트의 타이포그래피 스타일을 분석하여, **본문 줄간격(1.6)**과 **제목 자간(-0.025em), 줄간격(1.4)**을 세밀하게 재설정하여 가독성과 전문성을 동시에 끌어올림.
  - 기존 코드베이스에 남아있던 `.font-serif` 클래스들 또한 자동으로 Pretendard 기반의 꽉 찬 고딕 스타일로 렌더링되게 CSS 계층(layer) 오버라이딩 적용.

## [2026-03-12 11:30] 작업 내역 요약 - 폰트/그림자/타이포그래피 세부 정밀 매핑 (Phase 6: Tridge Style 완벽 이식)
- **이슈 바탕:** 기존 적용한 폰트에 더해, 글꼴 두께(Weight), 색상(Color 대비), 카드 그림자(Shadow Effects)까지 Tridge의 스타일을 정확히 차용하여 더욱 신뢰감 있고 현대적인 B2B 플랫폼 느낌을 구현하고자 함.
- **변경 사항:**
  - **폰트 두께 및 타이포 정리:** 랜딩 페이지 전체의 불필요한 `font-serif` 클래스를 전부 제거. 기존 `font-extrabold` (두께 800)로 설정되어 과하게 뚱뚱해 보이던 대제목들을 Tridge 방식에 맞춰 단단하고 세련된 `font-bold` (두께 700)로 수정.
  - **색상 대비 고도화:** 배경색(Deep Navy / Off-White)과의 대비를 고려하여 본문 텍스트 색상을 `text-[#4A4A4A]` (또는 호환되는 `text-muted-foreground`)로 조정. 장문의 글도 눈이 편안하게 읽히도록 처리.
  - **그림자(Shadow) 시스템 도입 (CSS Variables):** `globals.css` 내부에 Tridge 특유의 은은하면서도 깊이감 있는 그림자 효과인 `--shadow-tridge: 2px 4px 32px rgba(33, 43, 54, 0.16)` 와 호버 시 반응하는 `--shadow-tridge-hover` 를 새롭게 선언.
  - **인터랙션 적용:** 인사이트 딥다이브 리포트 카드, 대시보드 그래프 상자, 성공 사례(Success Cases) 카드 등에 커스텀 `shadow-tridge` 클래스를 일괄 적용하여 마우스 오버 시 카드가 자연스럽게 떠오르는 세련된 마이크로 인터랙션을 구현 완료.

## [2026-03-12 12:40] 작업 내역 요약 - 메인 페이지 모바일 레이아웃 겹침 현상 수정 (Phase 7)
- **이슈 바탕:** 화면 폭이 좁아지는 모바일 환경에서 "왜 Yoon&Partners인가" 영역과 "당신의 상황이 이 중 하나라면" 영역의 텍스트 레이어가 하단 다단 그리드 등과 겹치거나 깨지는 레이아웃 오버플로우 현상 발생.
- **변경 사항 (`app/page.tsx`):**
  - **왜 Yoon&Partners인가 (Differentiation Section):** 좌측 텍스트 컬럼과 우측 리스트 컬럼이 모바일에서는 수직으로 100% 폭을 차지하며 정상적으로 스태킹(Stacking)되도록 `w-full` 클래스를 기본으로 부여했습니다 (`w-full lg:w-5/12`, `w-full lg:w-7/12`). 또한 `sticky`와 `top` 속성을 데스크톱(`lg:`) 환경에서만 작동하도록 제한하여 모바일에서 강제로 스크롤 이탈하며 겹치는 문제를 해결했습니다.
  - **타겟 오디언스 (Target Audience Section):** 동일한 방식으로 좌측 문단과 우측 2x3 카드 그리드의 폭 할당을 `w-full lg:w-1/3 xl:w-1/4` 형태로 모바일 우선 100% 대응으로 수정하여 모바일 환경에서 위아래로 깔끔하게 떨어지도록 교정했습니다.

## [2026-03-12 12:56] 작업 내역 요약 - 전역 박스 디자인(Border Radius) 사각(Square) 통일 (Phase 9)
- **이슈 바탕:** 랜딩페이지 내의 카드, 버튼, 입력폼 등 요소마다 `rounded-sm`, `rounded-lg`, `rounded-xl` 등 모서리 둥글기 강도가 제각각 적용되어 있어 UI의 일관성이 떨어짐. Tridge 스타일의 권위 있고 정밀한 컨설팅 펌의 신뢰감을 주기 위해 Material Design 기반의 샤프한 사각 에지(Sharp Edge)로 통일 요청.
- **변경 사항:**
  - **CSS 변수 강제화 (`globals.css`):** Shadcn UI 컴포넌트들이 기본적으로 참조하는 `--radius-*` 변수를 전역적으로 `0rem`으로 재할당하여, 향후 추가되는 모든 기본 컴포넌트가 사각 형태로 렌더링되도록 원천 수정했습니다.
  - **Base Component 리팩터링 (`Button`, `Card`, `Input`, `Textarea`, `Checkbox`):** 해당 Base UI 파일 내 하드코딩 되어있던 `rounded-md` 계열의 Tailwind 유틸리티 클래스들을 모두 `rounded-none`으로 정비했습니다.
  - **개별 컴포넌트 하드코딩 제거:** `app/page.tsx`, `SuccessCases.tsx`, `DiagnosisForm.tsx`, `mypage/page.tsx` 등에 개별적으로 무작위 적용되어 있던 `.rounded-lg`, `.rounded-xl` 등의 클래스를 전부 제거 혹은 `.rounded-none`으로 수정하여, 아바타나 스텝 번호 같은 원형 뱃지를 제외한 모든 '박스형 컨텐츠/버튼'의 모서리 곡률을 완전히 일치(Straight Square)시켰습니다.

## [2026-03-13 11:05] 작업 내역 요약 - 다크모드 가독성 및 시각적 대비 최적화
- **이슈 바탕:** 다크모드 환경에서 테마 컬러간 강한 밝기 차이로 인한 가독성 저하 이슈 발생. 인간의 눈에 가장 편안한 대비값을 찾기 위한 시각적 최적화 요청.
- **분석 내용:**
  - `globals.css`의 다크모드 변수 구성에서 배경색과 전경색(글자색) 간 대비가 다소 차가워 WCAG 2.1 AA 기준의 편안한 대비 조정 필요.
  - 앱 전반(`app/page.tsx`, `DiagnosisForm.tsx`, `mypage/page.tsx` 등)에 라이트모드 기준의 헥사코드(`bg-[#fcfbf9]`, `bg-white` 등)가 요소별로 하드코딩되어 다크모드 동기화를 방해하고 있음.
- **변경 사항:**
  - `globals.css`: 다크모드 배경을 깊이감 있는 `Deep Navy Black (#050C17)`으로 변경하고, 눈부심 방지를 위해 텍스트 컬러를 `Soft Gray (#F8FAFC)`로 치환. 강조색(Accent)도 톤다운.
  - `app/page.tsx`: 렌딩페이지 각 주요 컨테이너의 하드코딩 색상(`bg-[#051320]`, `bg-[#f9f8f4]`, `bg-white`)을 모두 지우고 `bg-background`나 `bg-secondary` 같은 테마 공통 변수로 덮어씌움.
  - `DiagnosisForm.tsx` & `mypage/page.tsx`: 폼 내부의 `bg-[#fcfbf9]` 박스 및 `bg-white` 인풋을 모두 테마가 자동 전환되는 `bg-background` 변수로 이식 처리 완료.
  - `Navbar.tsx` & `mypage/page.tsx`: 다크모드에서 배경색과 겹쳐 보이지 않던 `text-secondary/80` 클래스를 테마 변수인 `text-foreground/80`으로 수정하여 시인성 확보.
